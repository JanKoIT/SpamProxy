services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-spamproxy}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
      POSTGRES_DB: ${POSTGRES_DB:-spamproxy}
      TZ: ${TZ:-Europe/Berlin}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
      - /etc/localtime:/etc/localtime:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-spamproxy}"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    volumes:
      - redis_data:/data
      - /etc/localtime:/etc/localtime:ro
    environment:
      TZ: ${TZ:-Europe/Berlin}
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  clamav:
    image: clamav/clamav:latest
    restart: unless-stopped
    volumes:
      - clamav_data:/var/lib/clamav
      - /etc/localtime:/etc/localtime:ro
    environment:
      TZ: ${TZ:-Europe/Berlin}
    healthcheck:
      test: ["CMD", "clamdscan", "--ping", "3"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 120s

  unbound:
    image: mvance/unbound:latest
    restart: unless-stopped
    volumes:
      - /etc/localtime:/etc/localtime:ro
    healthcheck:
      test: ["CMD", "drill", "@127.0.0.1", "google.com"]
      interval: 30s
      timeout: 5s
      retries: 3

  rspamd:
    build:
      context: ./docker/rspamd
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - ./docker/rspamd/local.d:/etc/rspamd/local.d
      - rspamd_data:/var/lib/rspamd
      - dkim_keys:/var/lib/rspamd/dkim
      - rspamd_keypairs:/etc/rspamd/keypairs
      - /etc/localtime:/etc/localtime:ro
    environment:
      TZ: ${TZ:-Europe/Berlin}
    depends_on:
      redis:
        condition: service_healthy
      clamav:
        condition: service_started
      unbound:
        condition: service_started
    # Healthcheck: ensures milter port 11332 is responsive. Docker will
    # auto-restart the container if it hangs (e.g. DNS/RBL stall).
    healthcheck:
      test: ["CMD-SHELL", "timeout 5 bash -c '</dev/tcp/127.0.0.1/11332' || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    # Ports nur intern - rspamd WebUI via Nginx oder: docker compose exec rspamd rspamadm ...

  postfix:
    build:
      context: ./docker/postfix
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      PROXY_HOSTNAME: ${PROXY_HOSTNAME:-proxy.example.com}
      POSTGRES_USER: ${POSTGRES_USER:-spamproxy}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
      POSTGRES_DB: ${POSTGRES_DB:-spamproxy}
      POSTGRES_HOST: postgres
      TZ: ${TZ:-Europe/Berlin}
    volumes:
      - postfix_queue:/var/spool/postfix
      - certs:/etc/ssl/certs/custom
      - postfix_logs:/var/log/postfix
      - /etc/localtime:/etc/localtime:ro
    ports:
      - "25:25"
      - "587:587"
    depends_on:
      postgres:
        condition: service_healthy
      rspamd:
        condition: service_healthy
      mail-service:
        condition: service_started

  mail-service:
    build:
      context: ./mail-service
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - postfix_logs:/var/log/postfix:ro
      - dkim_keys:/var/lib/rspamd/dkim
      - bayes_training:/var/lib/spamproxy/bayes-training
      - rspamd_keypairs:/etc/rspamd/keypairs
      - /etc/localtime:/etc/localtime:ro
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-spamproxy}:${POSTGRES_PASSWORD:-changeme}@postgres:5432/${POSTGRES_DB:-spamproxy}
      RSPAMD_URL: ${RSPAMD_URL:-http://rspamd:11333}
      RSPAMD_PASSWORD: ${RSPAMD_PASSWORD:-}
      AI_PROVIDER: ${AI_PROVIDER:-openai}
      AI_API_KEY: ${AI_API_KEY:-}
      AI_URL: ${AI_URL:-http://ollama:11434}
      AI_MODEL: ${AI_MODEL:-gpt-4o-mini}
      AI_ENABLED: ${AI_ENABLED:-true}
      SMTP_BACKEND_HOST: ${SMTP_BACKEND_HOST:-mail.example.com}
      SMTP_BACKEND_PORT: ${SMTP_BACKEND_PORT:-25}
      QUARANTINE_RETENTION_DAYS: ${QUARANTINE_RETENTION_DAYS:-30}
      TZ: ${TZ:-Europe/Berlin}
    # Port 8025 nur intern im Docker-Netzwerk, fuer dev: "8025:8025"
    depends_on:
      postgres:
        condition: service_healthy

  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-spamproxy}:${POSTGRES_PASSWORD:-changeme}@postgres:5432/${POSTGRES_DB:-spamproxy}
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET:-generate-a-secret-here}
      NEXTAUTH_URL: ${NEXTAUTH_URL:-http://localhost:3000}
      MAIL_SERVICE_URL: http://mail-service:8025
      ADMIN_EMAIL: ${ADMIN_EMAIL:-admin@example.com}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-changeme}
      TZ: ${TZ:-Europe/Berlin}
    volumes:
      - /etc/localtime:/etc/localtime:ro
    ports:
      - "${WEB_PORT:-3177}:3000"
    depends_on:
      postgres:
        condition: service_healthy
      mail-service:
        condition: service_started

volumes:
  postgres_data:
  redis_data:
  rspamd_data:
  clamav_data:
  dkim_keys:
  bayes_training:
  rspamd_keypairs:
  postfix_queue:
  postfix_logs:
  certs:
