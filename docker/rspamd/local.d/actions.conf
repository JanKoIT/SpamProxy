# rspamd soll NICHT selbst ablehnen - der Content Filter entscheidet.
# Die Thresholds hier steuern nur die rspamd Header-Markierung.
# "X-Spam: Yes" wird nur ab add_header-Score gesetzt.
reject = 9999;
add_header = 5;
greylist = null;
