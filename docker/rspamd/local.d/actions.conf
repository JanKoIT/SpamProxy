# rspamd soll NICHT selbst ablehnen - nur Header setzen.
# Die Entscheidung (deliver/quarantine/reject) trifft der Content Filter.
# reject auf absurd hohen Wert = praktisch deaktiviert
reject = 9999;
add_header = 3;
greylist = null;
